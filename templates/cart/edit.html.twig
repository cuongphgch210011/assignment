{% form_theme orderdetailForm 'bootstrap_5_layout.html.twig' %}
{% extends 'base.html.twig' %}

{% block title %}
	Shopping cart
{% endblock %}

{# {% block dropdown %}
    <div class="navbar-nav">
        <select onchange="window.location.href=this.value" id= "MyCategory_Btn" class ="btn btn-primary">
            <option class= "option">My Category</option>
            {% for c in category %}
                    <option value="{{path('category_detail' ,{'id': c.id }) }}">{{c.name}}</option>
            {% endfor %}
        </select>
    </div>
{% endblock %} #}

{% block body %}
	<div class="container col-md-6 text-center mt-3">
		<div class="col-lg-10">
			<h1 class="text text-primary">
				Shopping cart
			</h1>
			{{form_start (orderdetailForm) }}
            {{form_end (orderdetailForm) }}
			</div>
		</div>
	{% endblock %}

